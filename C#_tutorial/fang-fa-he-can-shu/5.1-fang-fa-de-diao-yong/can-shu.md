# 参数

参数是调用者向被调用方法传递数据的**变量**。

**形参**：声明函数或方法时定义的变量，用于接受数据的占位符

* 在定义函数或方法时，用于定义接受数据的数据类型
* 在函数或方法内部被使用，并在函数或方法被调用时被赋值

**实参**：调用函数或方法时实际传递的数据

* 调用时使用的实际的值
* 可以是常量、变量、表达式等。代表实际所使用的数据
* 当实参是表达式时，该表达式必须**具有返回值**
